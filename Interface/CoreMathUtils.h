#ifndef CORE_MATH_UTILS_H
#define CORE_MATH_UTILS_H

#define maxInt(a, b) ((a) > (b) ? (a) : (b))
#define minInt(a, b) ((a) < (b) ? (a) : (b))

#define absInt(a) ((a) > 0 ? (a) : -(a))

#endif // CORE_MATH_UTILS_H